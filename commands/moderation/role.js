module.exports = {
  name: 'role',
  description: 'Used by staff to give people roles',
  cooldown: 20,
  guildOnly: true,
  permission: 'KICK_MEMBERS',
  execute(message, args) {
    const { Client, Intents, MessageEmbed } = require('discord.js');
    
  }
}